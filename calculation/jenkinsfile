

pipeline {

    agent any

    environment {

        EC2_HOST = '16.171.129.98'

        EC2_USER = 'ubuntu'

    }

    stages {

        /*stage('Build') {

            steps {

                dir('calculation') {

                    sh 'ls'

                    sh 'docker build -t calculator -f Dockerfile .'

                }

            }

        }

 

        stage('Push to Docker Hub') {

            steps {

                sh "docker login -u jyothsnadevi1401@gmail.com -p 'Nanagaru@0114>>'"

                sh 'docker tag flask deekshat13/tdeeksha:flask'

                sh "docker push deekshat13/tdeeksha:flask"

            }

        }*/

 

        stage('Pull Docker Image on EC2') {

            steps {

                script {

                    withCredentials([file(credentialsId: 'pemkey2', variable: 'PRIVATE_KEY_FILE')]) {

                        sh """

                            chmod 400 \$PRIVATE_KEY_FILE

                            ssh -i \$PRIVATE_KEY_FILE -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \$EC2_USER@\$EC2_HOST<< EOF

                            //ssh -i your-ssh-key.pem -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ubuntu@your-ec2-host 'sudo docker pull your-image:tag'

 

                            sudo docker login -u deekshatadikamalla@gmail.com -p 'Deeksha@2000>>'

                            sudo docker pull deekshat13/tdeeksha:flask

                            //docker run -d -p 5000:5000 --name flask-app deekshat13/tdeeksha:flask

                            EOF

                        """

                    }

                }

            }

        }

    }

}
